import { LocationsCollection } from './locations-collection';

describe('LocationsCollection', () => {
  it('should create an instance', () => {
    expect(new LocationsCollection()).toBeTruthy();
  });
});
