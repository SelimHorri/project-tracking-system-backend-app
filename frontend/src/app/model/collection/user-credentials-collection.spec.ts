import { UserCredentialsCollection } from './user-credentials-collection';

describe('UserCredentialsCollection', () => {
  it('should create an instance', () => {
    expect(new UserCredentialsCollection()).toBeTruthy();
  });
});
