import { EmployeesCollection } from './employees-collection';

describe('EmployeesCollection', () => {
  it('should create an instance', () => {
    expect(new EmployeesCollection()).toBeTruthy();
  });
});
