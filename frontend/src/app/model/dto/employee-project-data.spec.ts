import { EmployeeProjectData } from './employee-project-data';

describe('EmployeeProjectData', () => {
  it('should create an instance', () => {
    expect(new EmployeeProjectData()).toBeTruthy();
  });
});
